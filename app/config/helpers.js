module.exports = {

	//isAdmin: guard.check('admin'),

	//isAllowed: (param) => guard.check(param),

	//formatDate: date => date.replace(/T/, ' ').replace(/\..+/, ''),

	/*formatDate: (date, format) => {
		format = format || 'YYYY-MM-DD HH:mm:ss';
		return moment(date).format(format);
	}*/

};